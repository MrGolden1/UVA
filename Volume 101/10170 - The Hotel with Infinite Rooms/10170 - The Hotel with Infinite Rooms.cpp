///// python
/*
import sys
import math
import re
from copy import deepcopy

#sys.stdin = open('in.txt','r')
#sys.stdout = open('out.txt','w')

while True:
    try:
        s , d = map(int , input().split())
    except:
        break

    d += ( s * (s - 1) / 2)
    delta  = 1 + 4 * 2 * d
    x = ( - 1 + math.sqrt(delta)) / 2
    print(math.ceil(x))
    */